# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (not used, but ignored for safety)
Pods/
Carthage/Build/

# Xcode project is generated by XcodeGen (project.yml is source of truth)
PatioFinder.xcodeproj/

# Local API key — never commit. The real key lives in Config.xcconfig locally;
# Config.example.xcconfig is the committed template.
Config.xcconfig

# Claude <-> Justin working folders and the reusable build guide (the guide
# holds account identifiers and device UDIDs and must never be committed).
Project Build Guide.md
_inbox/
_review/
